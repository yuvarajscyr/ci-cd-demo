name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run first CI step
        run: echo "CI pipeline started"

      - name: Run second step
        run: echo "Build completed successfully"

